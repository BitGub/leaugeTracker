<%= form_for(@sport) do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.hidden_field 'user_id', value:current_user.id %>

  <%= f.submit "Create Sport!" %>

<% end %>